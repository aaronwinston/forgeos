{
  "name": "forgeos",
  "version": "1.0.0",
  "description": "ForgeOS - Mission control for content",
  "private": true,
  "scripts": {
    "build": "cd apps/web && npm install && npm run build && mv .next ../../.next",
    "start": "cd apps/web && npm start",
    "dev": "cd apps/web && npm run dev"
  }
}
