/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a monorepo workaround - the .next folder is copied from apps/web
  // All configuration is inherited from apps/web/next.config.mjs
};

module.exports = nextConfig;
